import React from "react";
import { Sidebar, NewsContainer } from "../components";

const MainContent = () => {
  return (
    <div>
      <Sidebar />
      <NewsContainer />
    </div>
  );
};

export default MainContent;
